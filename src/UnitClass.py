class Unit:
    # TODO - this will be go-to object for later simulations
    # Unit will differ from Hero in that Unit will carry along state from simulations, and one of Unit's params will be the associated Hero obj
    # fields will be something like Hero (Hero obj), level, xp, current hp (probably only relevant for encounter sim,
    # which I don't see this ever getting to)
    pass